# TODO

- Update the tests to use the new mock shell so that we aren't cloning repos for all the git-dependency tests
- Generate a dynamic macro arguments with keyword args
- update all the help strings
- move the kwargs  to a bowerbird-nest repo
